{
  "private": true,
  "type": "module",
  "scripts": {
    "build": "astro build",
    "dev": "astro dev",
    "db:migrate": "bun ../../scripts/bookkit-migrate.ts --local",
    "demo": "bun run db:migrate && bun run dev"
  }
}
