---
"@reservajs/astro": minor
---

Admin settings: two-column form with every control always editable. Unsaved edits are flagged on the field and in a sticky Save bar. Message keys `settingStmt.*`, `admin.changeValue`, `admin.doneEditing` removed; `admin.unsaved`, `admin.unsavedHint` added.
